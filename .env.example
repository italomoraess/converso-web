# Base URL of the existing crm-api (NestJS)
NEXT_PUBLIC_API_URL=http://localhost:3000

# When true, screens render the bundled mock data instead of calling the API.
# Useful for design review without a running backend. Default: true.
NEXT_PUBLIC_USE_MOCK=true
